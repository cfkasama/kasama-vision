-- コメントに削除用ハッシュと論理削除の日時を追加
ALTER TABLE "Comment"
  ADD COLUMN IF NOT EXISTS "deleteKey" TEXT,
  ADD COLUMN IF NOT EXISTS "deletedAt" TIMESTAMP;

-- 取得クエリを楽にするためのインデックス（任意）
CREATE INDEX IF NOT EXISTS "idx_comment_postid_deletedat"
  ON "Comment" ("postId", "deletedAt");